# Copy to .env and fill in.
#
# Do NOT include the /api suffix — the API client
# (src/api/client.ts) appends /api/<resource> itself.
VITE_API_BASE_URL=http://localhost:8001

# Supabase Auth (frontend uses anon key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
